<UserControl x:Class="AutomaticWebInfoGetterWpfLib.Views.AddSettingView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:AutomaticWebInfoGetterWpfLib.Views"
             xmlns:ctrLib="clr-namespace:WpfCustomControlsByMe;assembly=WpfCustomControlsByMe"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <ResourceDictionary Source="../Resources/AddSettingViewDictionary.xaml"/>
    </UserControl.Resources>
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition Height="50"/>
        </Grid.RowDefinitions>
        <StackPanel >
            <TextBlock Style="{StaticResource TextInputAlignment}">URL</TextBlock>
            <TextBox Style="{StaticResource HeadingsStyle}"></TextBox>
            <TextBlock Style="{StaticResource TextInputAlignment}">XPath</TextBlock>
            <TextBox Style="{StaticResource HeadingsStyle}"></TextBox>
            <TextBlock Style="{StaticResource TextInputAlignment}">Frequency</TextBlock>
            <ctrLib:NumericUpDown/>
            <ComboBox HorizontalAlignment="Left" Width="auto">
                <ComboBoxItem>Only once now</ComboBoxItem>
                <ComboBoxItem>Each day</ComboBoxItem>
                <ComboBoxItem>Each week</ComboBoxItem>
                <ComboBoxItem>Each month</ComboBoxItem>
            </ComboBox>
            <TextBlock Style="{StaticResource TextInputAlignment}">Start date</TextBlock>
            <DatePicker HorizontalAlignment="Left"/>
        </StackPanel>
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition Width="auto"/>
                <ColumnDefinition Width="auto"/>
            </Grid.ColumnDefinitions>
            <Button Grid.Column="1">Add</Button>
            <Button Grid.Column="2">Cancel</Button>
        </Grid>
    </Grid>
</UserControl>
